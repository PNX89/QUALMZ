# QUALMZ

A researcher gets a fixed number of looks at the holdout. The count is a row with a unique
constraint, so re-running the same configuration does not buy another one.

Under construction. The pre-flight is measured and the code arrives commit by commit.
